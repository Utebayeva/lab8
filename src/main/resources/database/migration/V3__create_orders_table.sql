CREATE TABLE Orders (
                        order_id serial,
                        user_id serial,
                        product_id serial,
                        user_login varchar(255),
                        product_name varchar(255)
);