CREATE TABLE Users (
                       user_id serial,
                       user_login varchar(255),
                       user_password varchar(255),
                       user_balance float
);